% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rewriting.R
\name{final_pullback_complement}
\alias{final_pullback_complement}
\title{Compute the final pullback complement (FPC) for SqPO rewriting}
\usage{
final_pullback_complement(f, m)
}
\arguments{
\item{f}{ACSetTransformation A → B}

\item{m}{ACSetTransformation B → C}
}
\value{
List with D (ACSet), n (A → D), g (D → C)
}
\description{
Given f: A → B and m: B → C, compute D and morphisms n: A → D, g: D → C
such that the square is a final pullback.
}
\details{
For finite C-sets:
\itemize{
\item Elements c in C not in m(B): kept (multiplied if hom targets are cloned)
\item Elements c = m(b) where b in f(A): cloned (one copy per A-preimage)
\item Elements c = m(b) where b not in f(A): deleted
\item "Kept" elements whose hom targets were cloned are expanded (product rule)
}
}
