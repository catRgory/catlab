% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rewriting.R
\name{Rule}
\alias{Rule}
\alias{rule}
\title{Rewriting rules}
\usage{
Rule(
  l = ACSetTransformation(),
  r = ACSetTransformation(),
  monic = logical(0),
  semantics = character(0)
)

rule(l, r, monic = TRUE, semantics = "DPO")
}
\arguments{
\item{l}{ACSetTransformation I → L (left leg, pattern embedding)}

\item{r}{ACSetTransformation I → R (right leg, replacement embedding)}

\item{monic}{Logical; require match to be injective (default TRUE)}

\item{semantics}{Rewriting semantics: "DPO", "SPO", or "SqPO" (default "DPO")}
}
\value{
A Rule object
}
\description{
A rule is a span L ← I → R where:
\itemize{
\item L is the pattern (left-hand side)
\item I is the interface (preserved structure)
\item R is the replacement (right-hand side)
}
}
\details{
\code{Rule} is the S7 class; \code{rule()} is a convenience constructor.
}
\examples{
# DPO rule: delete an edge (keep both vertices)
L <- path_graph(2)           # 1 -> 2  (pattern: an edge)
I <- Graph(V = 2)            # 1  2    (interface: two vertices, no edge)
R <- Graph(V = 2)            # 1  2    (replacement: same)
l <- ACSetTransformation(list(V = c(1L, 2L), E = integer(0)), I, L)
r <- ACSetTransformation(list(V = c(1L, 2L), E = integer(0)), I, R)
rl <- rule(l, r)
# Apply to a triangle (3-cycle)
tri <- cycle_graph(3)
result <- rewrite(rl, tri)
ne(result)  # 2 (one edge deleted)
}
