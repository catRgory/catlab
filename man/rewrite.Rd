% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rewriting.R
\name{rewrite}
\alias{rewrite}
\title{Apply a rule to a graph, finding the first valid match}
\usage{
rewrite(rule, graph)
}
\arguments{
\item{rule}{A Rule object}

\item{graph}{Target ACSet}
}
\value{
The rewritten ACSet, or NULL if no match found
}
\description{
Apply a rule to a graph, finding the first valid match
}
\examples{
# Delete an edge via DPO rewriting
L <- path_graph(2)
I <- Graph(V = 2)
R <- Graph(V = 2)
l <- ACSetTransformation(list(V = c(1L, 2L), E = integer(0)), I, L)
r <- ACSetTransformation(list(V = c(1L, 2L), E = integer(0)), I, R)
rl <- rule(l, r)
g <- path_graph(3)
result <- rewrite(rl, g)
ne(result) # 1 (one edge removed)
}
