# Generated by roxygen2: do not edit by hand
import(S7)
import(acsets)

# Graphs
export(SchGraph, SchSymmetricGraph, SchReflexiveGraph, SchWeightedGraph, SchLabelledGraph)
export(Graph, WeightedGraph, LabelledGraph)
export(nv, ne, edge_src, edge_tgt, neighbors, add_vertex, add_vertices, add_edge)
export(path_graph, cycle_graph, complete_graph)

# Finite categories
export(FinCat, FinFunctor, ACSetTransformation)
export(is_natural, id_transformation, compose_transformations)

# Limits
export(coproduct, pushout, product, pullback)
export(equalizer, coequalizer)
export(terminal, initial, to_terminal, from_initial)

# Homomorphism finding
export(find_homomorphism, find_all_homomorphisms, is_homomorphic)

# Rewriting
export(Rule, rule, pushout_complement, rewrite_match, rewrite, get_matches)
export(cascading_complement, final_pullback_complement)

# Migration
export(delta_migrate, sigma_migrate)

# UWD
export(SchUWD, UWD, relation, box_spec, uwd)

# Typed ACSets
export(TypedACSet, typed_acset, typed_product, typed_coproduct)
export(flatten_typed, is_typed_morphism, discrete_typed)

# Structured Cospans
export(StructuredCospan, structured_cospan, discrete_acset, make_leg, open_acset)
export(compose_cospans, otimes_cospans, oapply_cospans)
export(cospan_apex, nlegs, foot_sizes)

# Graphviz / DiagrammeR
export(to_dot, to_graphviz, graph_to_dot, generic_to_dot, petri_to_dot, uwd_to_dot)
S3method(to_dot, default)
