# Terminal object: exactly one part per object type

Terminal object: exactly one part per object type

## Usage

``` r
terminal(schema)
```

## Arguments

- schema:

  A BasicSchema

## Value

An ACSet with 1 part per object
