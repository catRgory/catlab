# Convert an arbitrary ACSet to DOT format

Convert an arbitrary ACSet to DOT format

## Usage

``` r
generic_to_dot(acs, ...)
```

## Arguments

- acs:

  An ACSet

- ...:

  Additional arguments (currently unused)

## Value

A DOT format string
